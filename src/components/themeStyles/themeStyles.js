import React from 'react';
import { Global, css } from '@emotion/core';

const themeStyles = css`
  body {
  
  }
  .App-header {
 
  }
  .title-global {
  
  }
`;

export default () => <Global styles={themeStyles} />;
